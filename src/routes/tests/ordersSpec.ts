import app from '../../server';
import supertest from 'supertest';

const request = supertest(app)

describe("Test order endpoints", ()=>{
    describe("test show order endpoint", ()=>{
        it("test without giving a JWT", async(done)=>{
            

        })
    })

})